{
  "mcpServers": {
    "neural-amplifier": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "orchestrator",
        "neural-amplifier",
        "mcp",
        "--url",
        "http://127.0.0.1:8000"
      ]
    }
  }
}
